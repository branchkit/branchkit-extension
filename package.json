{
  "name": "branchkit-browser",
  "version": "0.1.0",
  "description": "BranchKit Browser — voice-navigable hint badges",
  "license": "SEE LICENSE IN LICENSE",
  "scripts": {
    "build": "npm run clean && npm run bundle && npm run copy-static",
    "bundle": "esbuild src/content.ts --bundle --format=iife --outfile=dist/content.js && esbuild src/bootstrap.ts --bundle --format=iife --outfile=dist/bootstrap.js && esbuild src/background.ts --bundle --format=esm --outfile=dist/background.js && esbuild src/offscreen.ts --bundle --format=iife --outfile=dist/offscreen.js && esbuild src/popup.ts --bundle --format=iife --outfile=dist/popup.js && esbuild src/options.ts --bundle --format=iife --outfile=dist/options.js",
    "dev": "npm run clean && mkdir -p dist && node scripts/dev.mjs",
    "watch": "npm run clean && mkdir -p dist && npm run copy-static && esbuild src/content.ts --bundle --format=iife --outfile=dist/content.js --watch & esbuild src/bootstrap.ts --bundle --format=iife --outfile=dist/bootstrap.js --watch & esbuild src/background.ts --bundle --format=esm --outfile=dist/background.js --watch & esbuild src/offscreen.ts --bundle --format=iife --outfile=dist/offscreen.js --watch & esbuild src/popup.ts --bundle --format=iife --outfile=dist/popup.js --watch & esbuild src/options.ts --bundle --format=iife --outfile=dist/options.js --watch",
    "clean": "rm -rf dist",
    "copy-static": "cp manifest.json dist/ && cp offscreen.html dist/ && cp popup.html dist/ && cp options.html dist/ && cp -r icons dist/",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.27.2",
    "happy-dom": "^20.9.0",
    "typescript": "^5.3.3",
    "vitest": "^4.1.5",
    "ws": "^8.20.1"
  },
  "dependencies": {
    "apca-w3": "^0.1.9"
  }
}
